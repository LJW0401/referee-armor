# 仓库协作规则

## 提交要求

- 每完成一个独立功能后，必须立即创建一次 Git 提交，再继续处理下一个功能。
- 提交信息继续遵循上级仓库规定的格式与语言要求。
- 包含非 ASCII 字符的提交信息必须写入 UTF-8 编码的文件，并通过 `git commit -F <文件>` 提交；不得通过 PowerShell 管道传递提交信息。
- 提交后必须使用 `git log -1 --format=%B` 验证提交说明未出现乱码；若验证失败，立即使用 UTF-8 文件修正最近一次提交说明。
